# BACKEND-GANACONTROL (Node + Express + Sequelize + MariaDB)

## Requisitos
- Docker + Docker Compose (recomendado)
- O Node.js + MariaDB local

## Variables de entorno
Copia `.env.example` a `.env` y ajusta credenciales.

## Levantar con Docker
```bash
docker compose up --build